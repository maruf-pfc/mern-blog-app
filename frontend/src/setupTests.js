import "@testing-library/jest-dom";
// import '@testing-library/jest-dom/extend-expect'; // Older way, current is just the import above

// You can add other global setup here if needed
// For example, cleaning up mocks after each test (though Vitest often handles this well)
// import { afterEach } from 'vitest';
// import { cleanup } from '@testing-library/react';
// afterEach(() => {
//   cleanup(); // RTL's cleanup function
// });
